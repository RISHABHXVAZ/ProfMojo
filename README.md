# ProfMojo 🚀

ProfMojo is a full-stack application built with:
- Spring Boot (Backend)
- React + Vite (Frontend)
- PostgreSQL (Database)
- Docker & Docker Compose

---

## 🔧 Prerequisites
- Docker Desktop installed
- Docker Compose enabled

(No Java, Node, or PostgreSQL needed)

---

## ▶ How to Run

1. Extract the ZIP
2. Open terminal in project root
3. Run:

```bash
docker compose up --build
